const scope = 'containers/HomePage';

export const LOADING = `${scope}/LOADING`;
export const ERROR = `${scope}/ERROR`;
export const LOAD_FILE = `${scope}/LOAD_FILE`;
export const IMPORT_DATA = `${scope}/IMPORT_DATA`;
